{
  "name": "langliu",
  "version": "0.0.1",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=24.18.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "biome format --write . --files-ignore-unknown=true"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.3",
    "lefthook": "^2.1.10",
    "turbo": "^2.10.4"
  },
  "packageManager": "pnpm@11.12.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
